[pytest]
pythonpath = .
testpaths = tests
addopts = -ra --strict-markers --strict-config
markers =
    unit: fast unit tests
    integration: tests requiring app wiring or DB integration
    security: authentication/authorization and hardening checks
    api: HTTP route and contract tests
