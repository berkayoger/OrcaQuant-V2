version: '3.9'
services:
  backend:
    image: python:3.12-slim
    working_dir: /app
    command: bash -lc "python -m http.server 5000"
    ports:
      - "5000:5000"
  frontend:
    image: node:20-alpine
    working_dir: /app
    command: sh -lc "echo 'frontend placeholder' && tail -f /dev/null"
